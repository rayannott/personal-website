# Business Card

A single-page personal business card website with 3D tilt effects,
holographic sheen, prismatic edge glow, and an animated particle background.

## Local development

Open `index.html` directly in a browser, or serve it with Python:

```bash
python -m http.server 8000
```

Then visit [http://localhost:8000](http://localhost:8000).
